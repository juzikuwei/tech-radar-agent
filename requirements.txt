# Runtime dependencies, pinned exactly for reproducible installs.
# To upgrade: bump the version here, run `python -m pip install -r requirements.txt`,
# then run the test suite before committing.
arxiv==4.0.0
sentence-transformers==5.6.0
chromadb==1.5.9
openai==1.109.1
python-dotenv==1.2.2
fastapi==0.139.0
uvicorn==0.51.0
httpx==0.28.1
mcp==1.28.1
